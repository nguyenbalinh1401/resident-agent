version: '3.8'

services:
  api:
    build: .
    container_name: pulse-chat-api
    ports:
      - "8081:8000"
    env_file:
      - .env
    restart: unless-stopped
  redis:
    image: redis:latest
    container_name: pulse-chat-redis
    ports:
      - "6379:6379"
    restart: unless-stopped 

